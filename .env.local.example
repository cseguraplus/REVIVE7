# Copia este archivo a .env.local y llena los valores de tu proyecto Supabase.
# Dashboard → Project Settings → API.
# La anon key es pública por diseño (se usa en el navegador, protegida por RLS) —
# de todas formas .env.local nunca se commitea (.gitignore).

VITE_SUPABASE_URL=https://ntsecphrxxdcovhuweeo.supabase.co
VITE_SUPABASE_ANON_KEY=
